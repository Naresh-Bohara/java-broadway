package abstraction;

public class Abstraction {
	/*
	 * -------------------- Abstration ---------------------
	 * # process of hiding implementations details in the program.
	 * # To achieve abstraction:
	 * 
	 * 		1. Abstract class:
	 * 			-> class which contains abstract (unimplemented) methods and non-abstract methods.
	 * 				# Abstract method:
	 * 					-> method which doesn't have body/implementation
	 * 					-> syntax:
	 * 							abstract retrun_type method_name(arg1, arg2, ...,argn)
	 * 					-> e.g:
	 * 							abstract void print();
	 * 			-> every abstract class must have at least one child class and child class must override abstract methods.
	 * 			-> we can't create object of abstract class beacause it has abstract methods which are not implemented.
	 * 			
	 * 			
	 * 
	 * 		2. Interface
	 * 
	 * 
	 * 
	 */
}
