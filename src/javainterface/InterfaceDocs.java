package javainterface;

public class InterfaceDocs {
	/*
	 * ----------------- Interface ----------------------
	 * # To achieve abstraction.
	 * # same as class which contains which contains public abstract methods and public static final 
	 * 	 variables (i.e constants).
	 * # every interface must have at least one implementation class and impl. class must ovrride abstract methods.
	 * # we can't create object.
	 * # 100% abstraction
	 * # multiple inheritance support in java with innterface.
	 * # syntax:
	 * 		interface interface_name {
	 * 
	 * 			// public abstract methods
	 * 			
	 * 			// public static final variables
	 * 
	 * 		}
	 */
}
