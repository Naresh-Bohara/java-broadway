package array;

public class ArrayDocs {
/*
 * --------------- Array -------------------
 * int a = 500;
 * 	   a = 600;
 * sysout(a); //? -> 800
 * 
 * x		y		z
 * 100	200		300 ---> we can use variables 
 * what if: 
 * 100, 200, 300, 400, 500, 600, 125, 123, 343, ........upto 700 values
 * 
 *  # collection of homogeneous data. its is also called static collection
 *  # collection/group of multiple values of same data type with fixed size.
 * 	# data stored on the basis of indices(i.e. index start from 0).
 * 	# contains duplicate data also.
 * 	# random access. - mean we can access pop push at any index...
 * 	# contiguous memory allocation -> [][][][][]-- store like a chain , mean like dog chain ...
 * 	# array is an object in java (implicitly)
 * 
 * 	# Syntax: 
 * 		data_type array_name[] =  new data_type[size];
 * 		or
 * 		new data_type[size];
 * 
 * # eg:
 * 		String cities[] = new String[100];
 * 		cities[0] = "ktm";
 * 
 * 		float []heights = new float[800];
 * 
 *  
 *  
 */
}
